/**
 * Crypto helpers placeholder.
 * Phase 2+ will add Cryptomus signature verification and related utilities.
 * Do not implement payment logic in Phase 1.
 */

export function notImplementedCryptoHelper(name = 'crypto helper') {
  throw new Error(`${name} is reserved for Phase 2+ (Cryptomus payments).`);
}

export default {
  notImplementedCryptoHelper,
};
