/**
 * Wallet helpers placeholder.
 * Phase 2+ will implement Seller Internal Wallet ledger math.
 * Do not implement wallet/escrow business logic in Phase 1.
 */

export function notImplementedWalletHelper(name = 'wallet helper') {
  throw new Error(`${name} is reserved for Phase 2+ (Seller Internal Wallet).`);
}

export default {
  notImplementedWalletHelper,
};
