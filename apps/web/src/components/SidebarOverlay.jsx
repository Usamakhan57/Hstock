import React from 'react';

/**
 * @deprecated Seller Workspace is a profile dropdown — no drawer overlay.
 * Kept as a no-op export so any stale import cannot resurrect sheet UX.
 */
const SidebarOverlay = () => null;

export default SidebarOverlay;
